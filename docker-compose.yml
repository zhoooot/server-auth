version: '3'

services:
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: test
    ports:
      - "30000:3306"
  redis:
    image: redis:5.0.5
    ports:
      - "30001:6379"
